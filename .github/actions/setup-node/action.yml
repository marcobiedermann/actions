name: Setup Node.js
description: Sets up Node.js using the version from .nvmrc with npm caching enabled.
runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v6
      with:
        cache: "npm"
        node-version-file: ".nvmrc"
