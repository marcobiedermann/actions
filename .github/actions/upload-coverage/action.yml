name: Upload Coverage
description: Thin wrapper around codecov/codecov-action that uploads coverage reports to Codecov.

inputs:
  codecov_token:
    description: Codecov access token
    required: true

runs:
  using: "composite"
  steps:
    - uses: codecov/codecov-action@v5
      with:
        token: ${{ inputs.codecov_token }}
